services:
  blog:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    # 不手动设置 NODE_ENV，让 next dev / next build 自动管理
    env_file:
      - .env.local
    restart: unless-stopped
